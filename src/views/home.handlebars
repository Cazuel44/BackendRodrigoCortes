<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div>
        <h1>Lista de Productos</h1>
        <ul id="products">
            {{#each productos}}
            <li id="{{id}}">{{title}} - {{description}} - {{code}} - {{price}} - {{status}} - {{stock}} - {{category}} - {{thumbnails}} - {{id}}</li>
            {{/each}}
        </ul>
        {{> saludo}}
        {{> chat}}
    </div>
    

    <script src="/socket.io/socket.io.js"></script>
    <script src="/scripts.js"></script>
</body>
</html> 
 
 
 
 